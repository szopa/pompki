<%= form_for(@series) do |f| %>
  <% if @series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% @series.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if false %>
  <div class="fields">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :sum %><br />
    <%= f.number_field :sum %>
  </div>
<% end %>
<ol>
  <%= f.fields_for :pushups do |builder| %>
    <%= render 'pushup_fields', :f => builder %>
  <% end %>
  <p><%= link_to_add_fields "Add series", f, :pushups %></p>
</ol>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
